# Nankai_synchronization

A collection of model results for modelling synchronization between faults. This provides further support for the results derived with simple spring-block slider models.

## Data

The data is stored on box.

## Requirements

The requirements are stored in the `environment.yml` file.

## Installation

The following command will install the requirements:

```bash
conda env create -f environment.yml
```